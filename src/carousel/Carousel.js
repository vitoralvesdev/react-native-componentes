import React, { Component, PropTypes } from 'react';
import {
    View,
    Text,
    StyleSheet,
    ScrollView
} from 'react-native';

export default class Carousel extends Component {
    render() {
        return(
            <View>
                <Text> Carousel </Text>
            </View>
        )
    }
}